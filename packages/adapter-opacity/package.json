
{
    "name": "@elizaos/adapter-opacity",
    "version": "0.0.1-alpha.1",
    "description": "Opacity Protocol adapter for Eliza",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsup src/index.ts --format esm --dts",
        "lint": "eslint --fix --cache .",
        "watch": "tsc --watch",
        "dev": "tsup src/index.ts --format esm --dts --watch"
    },
    "dependencies": {
        "@elizaos/core": "workspace:*",
        "dotenv": "^16.4.5"
    },
    "devDependencies": {
        "tsup": "^8.3.5"
    }
}