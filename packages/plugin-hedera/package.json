{
    "name": "@elizaos/plugin-hedera",
    "version": "0.1.9-alpha.1",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "dependencies": {
        "@elizaos/core": "workspace:*",
        "hedera-agent-kit": "^1.0.2",
        "tsup": "8.3.5"
    },
    "scripts": {
        "build": "tsup --format esm --dts",
        "dev": "tsup --format esm --dts --watch",
        "test": "vitest run"
    },
    "devDependencies": {
        "@chain-registry/types": "^0.50.44"
    }
}
