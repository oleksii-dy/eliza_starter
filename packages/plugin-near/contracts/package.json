{
  "name": "near-plugin-contracts",
  "version": "1.0.0",
  "type": "module",
  "description": "NEAR smart contracts for the ElizaOS NEAR plugin",
  "scripts": {
    "install-all": "cd escrow-js && npm install && cd ../messaging-js && npm install",
    "build": "node build.js",
    "test": "./test-all.sh",
    "demo": "npm run build && node demo-contracts.js"
  },
  "devDependencies": {
    "near-workspaces": "^5.0.0"
  }
}
