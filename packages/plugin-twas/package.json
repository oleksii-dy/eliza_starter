{
  "name": "@elizaos/plugin-twas",
  "version": "0.1.9",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@elizaos/core": "workspace:*",
    "@geekyrocks/alkahest-ts": "0.1.3-m1",
    "axios": "^1.7.9",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  },
  "scripts": {
    "build": "tsup --format esm --dts",
    "dev": "tsup --format esm --dts --watch",
    "test": "vitest run",
    "lint": "biome lint .",
    "lint:fix": "biome check --apply .",
    "format": "biome format .",
    "format:fix": "biome format --write ."
  }
}