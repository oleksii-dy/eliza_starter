import prettierConfig from '../core/configs/prettier/prettier.config.js';

/**
 * Prettier configuration for @elizaos/client
 * Uses the standardized prettier configuration from core/configs
 */
export default {
  ...prettierConfig,
  // Client-specific overrides can be added here if needed
};