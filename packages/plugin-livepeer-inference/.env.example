# Livepeer Gateway Configuration
# Required: URL of the Livepeer Gateway API
LIVEPEER_GATEWAY_URL=https://gateway.livepeer.com/api

# Optional: API key for Livepeer Gateway authentication (defaults to "eliza-app-llm")
LIVEPEER_API_KEY=your-api-key-here

# Optional: Model selection
# Default model used for inference if LIVEPEER_LARGE_MODEL is not specified
LIVEPEER_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct

# Optional: Alternative model for larger contexts (overrides LIVEPEER_MODEL)
# LIVEPEER_LARGE_MODEL=meta-llama/Meta-Llama-3.1-70B-Instruct

# Optional: Generation parameters
# Controls randomness in generated text (0.0-1.0)
LIVEPEER_TEMPERATURE=0.6

# Maximum tokens to generate in responses
LIVEPEER_MAX_TOKENS=2048
