{
  "name": "@elizaos/plugin-dummy-services",
  "version": "1.0.12",
  "type": "module",
  "description": "Comprehensive dummy services and interface testing plugin for ElizaOS development and CI/CD",
  "keywords": [
    "elizaos",
    "dummy",
    "mock",
    "testing",
    "services",
    "interfaces",
    "development"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch",
    "clean": "rm -rf dist && rm -rf .turbo",
    "lint": "eslint src --fix && prettier --write ./src",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@elizaos/core": "workspace:*",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^8.26.0",
    "@typescript-eslint/parser": "^8.26.0",
    "eslint": "^9.22.0",
    "prettier": "3.5.3",
    "typescript": "5.8.3",
    "vitest": "^2.0.0"
  },
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "@elizaos/core": "workspace:*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/elizaos/eliza.git",
    "directory": "packages/plugin-dummy-services"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "gitHead": "646c632924826e2b75c2304a75ee56959fe4a460",
  "agentConfig": {
    "pluginType": "elizaos:plugin:1.0.0",
    "pluginParameters": {
      "DISCORD_APPLICATION_ID": {
        "type": "string",
        "description": "Discord application/client ID used for authenticating the bot with Discord API.",
        "required": true
      },
      "DISCORD_API_TOKEN": {
        "type": "string",
        "description": "Discord bot token used to authenticate and connect to Discord API.",
        "required": true
      },
      "CHANNEL_IDS": {
        "type": "string",
        "description": "Optional comma-separated list of Discord channel IDs the bot is allowed to operate in; if unset the bot works in all channels.",
        "required": false
      },
      "DISCORD_TEST_CHANNEL_ID": {
        "type": "string",
        "description": "Discord channel ID for running tests; used if not supplied in runtime settings.",
        "required": false
      },
      "DISCORD_VOICE_CHANNEL_ID": {
        "type": "string",
        "description": "Specifies the Discord voice channel ID that the bot should attempt to join before scanning for other available channels.",
        "required": false
      }
    }
  }
}
